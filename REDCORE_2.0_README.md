# REDCORE 2.0 — 실제 쇼핑몰 전환용 구조

## 현재 포함된 것
- 기존 REDCORE 프론트엔드
- `server/` Node.js + Express API 스타터
- 주문 생성 API
- 주문 조회 API
- 관리자 주문 목록 API
- 관리자 주문 상태 변경 API
- 주문 상태: 결제대기 / 결제완료 / 상품준비 / 배송중 / 배송완료 / 취소

## 실행
1. Node.js LTS 설치
2. 터미널에서 `server` 폴더로 이동
3. `npm install`
4. `npm start`
5. `http://localhost:3000/api/health` 접속해서 `{"ok":true,...}` 확인

## 중요
이 서버는 **결제 테스트 전 단계의 스타터**다.
주문 데이터가 메모리에 저장되므로 서버를 재시작하면 사라진다.

실제 판매 전에는:
- PostgreSQL/Supabase 같은 영구 DB
- 관리자 인증
- HTTPS
- 환경변수/비밀키 관리
- PG 결제 연동
- 결제 위변조 검증
- 개인정보 보호/법적 고지
등을 추가해야 한다.

특히 카드번호나 PG 비밀키를 프론트엔드 코드에 직접 넣으면 안 된다.

## 다음 단계
PG 가입 조건과 미성년자/사업자 조건을 보호자와 확인한 뒤,
실제 사용할 PG의 공식 API 방식에 맞춰 `결제 준비 → 결제 승인 → 서버 검증 → 주문 PAID 처리`를 연결한다.
